(function(root){
  const Robot=root.BizimSkorRobotPrediction;
  const cache=new Map();
  async function suggestion(fixture){if(cache.has(fixture.id))return cache.get(fixture.id);let value={homeScore:1,awayScore:1};try{const q=await root.sb.rpc('get_match_statistics',{p_fixture_id:Number(fixture.id)});const snapshot=Array.isArray(q.data)?q.data[0]:q.data;if(q.error)throw q.error;value=Robot.suggest(snapshot)}catch(_){ }cache.set(fixture.id,value);return value}
  function superFixtures(){return root.BizimSkorPredictionContext?.().fixtures||[]}
  function championsFixtures(){return root.BizimSkorChampionsUI?.getFixtures?.()||[]}
  function config(){const superRows=[...document.querySelectorAll('#fx .m')],championRows=[...document.querySelectorAll('#championsFixtures .champions-match')];if(superRows.length)return{kind:'super',fixtures:superFixtures(),rows:superRows,inputIds:(f,i)=>['h'+i,'a'+i],host:document.getElementById('predictionState'),anchor:document.getElementById('fx')?.parentElement};if(championRows.length)return{kind:'champions',fixtures:championsFixtures(),rows:championRows,inputIds:f=>['clh'+f.id,'cla'+f.id],host:document.getElementById('championsState'),anchor:document.getElementById('championsFixtures')?.parentElement};return null}
  async function applyOne(fixture,index,settings,onlyEmpty=false){const [homeId,awayId]=settings.inputIds(fixture,index),home=document.getElementById(homeId),away=document.getElementById(awayId);if(!home||!away||(onlyEmpty&&(home.value!==''||away.value!=='')))return false;const value=await suggestion(fixture);home.value=String(value.homeScore);away.value=String(value.awayScore);home.dataset.robotScore=home.value;away.dataset.robotScore=away.value;return true}
  function ensureStyles(){if(document.getElementById('robotPredictionStyles'))return;const style=document.createElement('style');style.id='robotPredictionStyles';style.textContent='.match-stats-button{grid-column:1/4!important;width:100%!important;margin:7px 0 1px!important;padding:9px 7px!important;font-size:12px!important}.robot-prediction-button{grid-column:4/6;width:100%;margin:7px 0 1px;padding:9px 7px;border:1px solid #86efac;border-radius:10px;background:#f0fdf4;color:#15803d;font-size:12px;font-weight:900;line-height:1.15}.robot-prediction-all{width:100%;margin:0 0 8px;padding:10px;border:1px solid #86efac;border-radius:10px;background:#f0fdf4;color:#15803d;font-size:13px;font-weight:900}.robot-prediction-rule{margin:12px 0 0;padding:10px 12px;border-radius:10px;background:#f8fafc;color:#475569;font-size:12px;line-height:1.45}.robot-prediction-rule b{color:#166534}';document.head.appendChild(style)}
  function ruleText(kind){return kind==='champions'?'<b>🤖 Robotun önerisi:</b> En az 6 maçı kendin tahmin etmelisin. Robot en fazla 12 maçta kullanılabilir.':'<b>🤖 Robotun önerisi:</b> En az 3 maçı kendin tahmin etmelisin. Robot en fazla 6 maçta kullanılabilir.'}
  async function decorate(){const settings=config();if(!settings||!Robot)return;ensureStyles();settings.rows.forEach((row,index)=>{if(row.querySelector('[data-robot-one]'))return;const fixture=settings.fixtures[index];if(!fixture)return;const button=document.createElement('button');button.type='button';button.dataset.robotOne='1';button.className='robot-prediction-button';button.textContent='🤖 Robotun Önerisi';button.addEventListener('click',()=>applyOne(fixture,index,settings));row.appendChild(button)});if(settings.host&&!settings.host.querySelector('[data-robot-all]')){const all=document.createElement('button');all.type='button';all.dataset.robotAll='1';all.className='robot-prediction-all';all.textContent='🤖 Robotun Önerisini Boş Maçlara Uygula';all.addEventListener('click',async()=>{for(let i=0;i<settings.fixtures.length;i++)await applyOne(settings.fixtures[i],i,settings,true)});settings.host.appendChild(all)}const rule=settings.anchor?.querySelector?.('[data-robot-prediction-rule]');if(rule)rule.innerHTML=ruleText(settings.kind);else if(settings.anchor){const note=document.createElement('p');note.dataset.robotPredictionRule='1';note.className='robot-prediction-rule';note.innerHTML=ruleText(settings.kind);settings.anchor.appendChild(note)}}
  function mount(){const run=()=>setTimeout(decorate,0);new MutationObserver(run).observe(document.body,{childList:true,subtree:true});document.addEventListener('change',run);run()}
  root.BizimSkorRobotUI=Object.freeze({suggestion,decorate,mount});
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount,{once:true});else mount();
})(typeof globalThis!=='undefined'?globalThis:this);
