const test=require('node:test');const assert=require('node:assert/strict');
const B=require('./ranking-movement-bootstrap');
const players=[{id:1,name:'A',created_at:'2026-01-01',is_active:true},{id:2,name:'B',created_at:'2026-01-02',is_active:true},{id:3,name:'C',created_at:'2026-01-03',is_active:true}];
const meta=B.metaMaps(players,[]);
test('son sonucu dislayarak haftalik onceki sirayi yeniden kurar',()=>{const preds=[{player_name:'A',fixture_id:1,week:5,home_score:1,away_score:0},{player_name:'B',fixture_id:1,week:5,home_score:0,away_score:0},{player_name:'C',fixture_id:1,week:5,home_score:2,away_score:2}];const before=B.rankWeek(preds,[],meta,5,false);const after=B.rankWeek(preds,[{fixture_id:1,home_score:1,away_score:0}],meta,5,false);assert.deepEqual(before.map(x=>x.name),['A','B','C']);assert.equal(after[0].name,'A');assert.equal(after[0].pts,4)});
test('arkadas liginde son mac katkisini mevcut toplamdan cikarir',()=>{const current=[{player_name:'A',points:9,exact_count:1,correct_count:2,score_start_week:5},{player_name:'B',points:8,exact_count:1,correct_count:1,score_start_week:5}];const preds=[{player_name:'A',fixture_id:45,week:5,home_score:0,away_score:0},{player_name:'B',fixture_id:45,week:5,home_score:1,away_score:0}];const before=B.rankFriendBefore(current,{id:45,week:5},{fixture_id:45,home_score:0,away_score:0},preds,meta);assert.equal(before.find(x=>x.name==='A').pts,5);assert.equal(before.find(x=>x.name==='B').pts,8)});
test('arena onceki lig sirasi performans ve baglayicilarla kurulur',()=>{const aggs=new Map([['a',{name:'A',perfSum:70,valid:1,ex:1,raw:7}],['b',{name:'B',perfSum:80,valid:1,ex:0,raw:8}]]);const rows=B.rankArenaBefore([{player_name:'A'},{player_name:'B'}],aggs,meta);assert.deepEqual(rows.map(x=>x.name),['B','A']);assert.deepEqual(rows.map(x=>x.rank),[1,2])});
