(function(root,factory){
  const api=factory(root);
  if(typeof module==='object'&&module.exports)module.exports=api;
  else{root.BizimSkorRankingMovementUI=api;api.mount()}
})(typeof globalThis!=='undefined'?globalThis:this,function(root){
  const DEFAULT_SEASON='2026/27';
  function text(node){return String(node?.textContent||'').trim()}
  function number(value){const m=String(value??'').match(/-?\d+(?:[.,]\d+)?/);return m?Number(m[0].replace(',','.')):0}
  function rankingTargets(){return[['generalBoard','general'],['weeklyRankingBoard','weekly'],['championsRankingBoard','champions'],['championsWeeklyRankingBoard','weekly'],['nationsRankingBoard','nations'],['nationsWeeklyRankingBoard','weekly'],['friendLeagueRanking','friend']]}
  function seasonFor(host){return String(host?.dataset?.rankingSeason||host?.closest?.('[data-ranking-season]')?.dataset?.rankingSeason||root.BizimSkorSeason||DEFAULT_SEASON)}
  function contextKey(host,kind){const season=seasonFor(host);if(kind==='general')return`season:${season}:general`;if(kind==='weekly'){const title=text(root.document?.getElementById('weeklyRankingTitle'))||'weekly',board=String(host?.id||'weekly');return`season:${season}:weekly:${board}:${title.toLocaleLowerCase('tr-TR')}`}if(kind==='friend')return`season:${season}:friend:${String(host?.dataset?.rankingContext||'current')}`;if(kind==='arena'){const shell=host.closest?.('.league-shell'),heading=text(shell?.querySelector('h2'))||'arena',period=String(host?.dataset?.rankingPeriod||shell?.dataset?.rankingPeriod||'current');return`season:${season}:arena:${period}:${heading.toLocaleLowerCase('tr-TR')}`}return`season:${season}:${kind}`}
  function tableRows(host){return[...(host?.querySelectorAll?.('table tr')||[])].slice(1).map((tr,index)=>{const cells=tr.querySelectorAll('td');if(cells.length<2)return null;const name=text(cells[1].querySelector?.('.bs-player-profile-link'))||text(cells[1]);return{name,rank:number(cells[0])||index+1,metric:[...cells].slice(2).map(cell=>text(cell)).join('|'),rankNode:cells[0]}}).filter(Boolean)}
  function arenaRows(host){return[...(host?.querySelectorAll?.('.league-row')||[])].map((row,index)=>({name:text(row.querySelector('.league-player')).replace(/^Sen\s*•\s*/i,''),rank:number(row.querySelector('.league-rank'))||index+1,metric:[text(row.querySelector('.league-performance')),text(row.querySelector('.league-rounds'))].join('|'),rankNode:row.querySelector('.league-rank')})).filter(row=>row.name&&row.rankNode)}
  function revisionFor(host,rows,movement){return String(host?.dataset?.rankingRevision||host?.closest?.('[data-ranking-revision]')?.dataset?.rankingRevision||movement.fingerprint(rows))}
  function reconcileBadge(rankNode,current,movement){const existing=rankNode?.querySelector?.('.bs-rank-move')||null;if(!current){existing?.remove?.();return!!existing}rankNode?.classList?.add?.('bs-rank-cell');const up=current.direction==='up',expectedText=up?'▲':'▼',expectedClass=up?'bs-rank-up':'bs-rank-down';if(existing&&text(existing)===expectedText&&existing.classList?.contains?.(expectedClass))return false;existing?.remove?.();rankNode?.insertAdjacentHTML?.('beforeend',movement.badge(current));return true}
  function apply(host,kind){const movement=root.BizimSkorRankingMovement;if(!movement||!host)return false;movement.ensureStyles?.();const rows=kind==='arena'?arenaRows(host):tableRows(host);if(!rows.length)return false;const key=contextKey(host,kind),bootstrap=root.BizimSkorRankingMovementBootstrap,seed=bootstrap?.seedFor?.(key)||null,revision=bootstrap?.revisionFor?.(key)||revisionFor(host,rows,movement),state=movement.trackWithSeed?movement.trackWithSeed(key,rows,revision,seed):movement.track(key,rows,revision);rows.forEach(row=>reconcileBadge(row.rankNode,movement.movementFor(state,row.name),movement));return true}
  function scan(doc=root.document){if(!doc)return false;let changed=false;rankingTargets().forEach(([id,kind])=>{const host=doc.getElementById(id);if(host)changed=apply(host,kind)||changed});doc.querySelectorAll?.('#leagueSystemPanel .league-table-wrap').forEach(host=>{changed=apply(host,'arena')||changed});return changed}
  function mount(){const doc=root.document,Observer=root.MutationObserver;if(!doc||!Observer||root.__bizimSkorRankingMovementObserver)return false;let timer=null;const schedule=()=>{root.clearTimeout?.(timer);timer=root.setTimeout?.(()=>scan(doc),40)};const start=async()=>{try{await Promise.race([root.BizimSkorRankingMovementBootstrap?.whenReady?.()||Promise.resolve(),new Promise(resolve=>root.setTimeout?.(resolve,2500))])}catch(_){}scan(doc);const observer=new Observer(schedule);observer.observe(doc.body,{childList:true,subtree:true,characterData:true});root.__bizimSkorRankingMovementObserver=observer};if(doc.readyState==='loading')doc.addEventListener('DOMContentLoaded',start,{once:true});else start();return true}
  return Object.freeze({number,tableRows,arenaRows,rankingTargets,seasonFor,contextKey,revisionFor,reconcileBadge,apply,scan,mount});
});
