(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;else root.BizimSkorInviteLeaderboard=api;})(typeof globalThis!=='undefined'?globalThis:this,function(){
function buildInviteLeaderboard(rows,period={type:'season'}){const filtered=(rows||[]).filter(r=>{if(period.type!=='month')return true;const d=new Date(r.createdAt);return d.getUTCFullYear()===Number(period.year)&&d.getUTCMonth()+1===Number(period.month)});const map=new Map();for(const r of filtered){const k=String(r.inviterId);const prev=map.get(k)||{inviterId:k,name:r.name||k,count:0};prev.count++;map.set(k,prev)}return [...map.values()].sort((a,b)=>b.count-a.count||a.name.localeCompare(b.name,'tr')).map((x,i)=>({...x,rank:i+1}));}
return {buildInviteLeaderboard};});
