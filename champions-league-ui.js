(function(){
  const season='2026/27',week=2,CHAMPIONS_OPPORTUNITY_ID=33;
  let fixtures=[],editing=false,weeklyCompetition='super_lig',robotPredictions={};
  const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
  function mount(){
    const tabs=document.querySelector('.tabs'),pred=document.getElementById('pred');
    if(!tabs||!pred)return;
    if(!document.getElementById('championsPred')){
      tabs.insertAdjacentHTML('beforeend','<button class="tab champions-tab" data-tab="championsRanking">🏆 Şampiyonlar Ligi Sıralaması</button>');
      pred.insertAdjacentHTML('afterend','<section id="championsPred" class="hide"><div class="champions-shell"><div class="champions-hero"><span>✦ 2026/27 AVRUPA GECESİ ✦</span><b>Şampiyonlar Ligi • 2. Hafta</b><p class="small">Doğru sonuç +1 • Tam skor ekstra +3 • İlk maçla birlikte tüm tahminler kilitlenir.</p></div><div id="championsState"></div><div id="championsFixtures"></div><button id="championsSave" class="full p">Şampiyonlar Ligi Tahminlerimi Kaydet ⭐</button></div></section><section id="championsRanking" class="hide"><div class="champions-shell"><div class="champions-hero"><span>✦ BİZİM SKOR AVRUPA GECESİ ✦</span><b>Şampiyonlar Ligi Sıralaması</b><p class="small">Şampiyonlar Ligi sezon puanları Süper Lig’den tamamen bağımsızdır.</p></div><div id="championsRankingBoard"></div><h3>Maç Sonuçları</h3><p class="small">Tam skoru bilenler ve doğru sonucu bilen oyuncu sayısı.</p><div id="championsMatchResults"></div></div></section>');
      const rankingButton=tabs.querySelector('[data-tab="championsRanking"]');
      rankingButton.onclick=async()=>{document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));document.querySelectorAll('section').forEach(x=>x.classList.add('hide'));rankingButton.classList.add('active');document.getElementById('championsRanking').classList.remove('hide');await loadRanking()};
      document.querySelectorAll('.tab:not([data-tab="championsRanking"])').forEach(item=>item.addEventListener('click',()=>{document.getElementById('championsPred').classList.add('hide');document.getElementById('championsRanking').classList.add('hide')}));
      document.getElementById('championsSave').onclick=savePrediction;
      setInterval(()=>{const ranking=document.getElementById('championsRanking'),weeklyPanel=document.getElementById('championsWeeklyRankingPanel');if(document.visibilityState==='visible'&&ranking&&!ranking.classList.contains('hide'))loadRanking();if(document.visibilityState==='visible'&&weeklyCompetition==='champions_league'&&weeklyPanel&&!weeklyPanel.classList.contains('hide'))loadChampionsWeeklyRanking()},60000);
    }
    mountWeeklyRankingPanel();
    document.querySelector('[data-tab="live"]')?.addEventListener('click',()=>setTimeout(()=>{mountWeeklyRankingPanel();if(weeklyCompetition==='champions_league')setWeeklyCompetition('champions_league')},250));
  }
  function openPrediction(){document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));document.querySelectorAll('section').forEach(x=>x.classList.add('hide'));document.getElementById('championsPred')?.classList.remove('hide');return loadPrediction()}
  function currentWeek(){return week}
  function timeText(value){return new Intl.DateTimeFormat('tr-TR',{timeZone:'Europe/Istanbul',hour:'2-digit',minute:'2-digit',hour12:false}).format(new Date(value)).replace(':','.')}
  function rowFixture(row){return{...row,id:Number(row.fixture_id)}}
  function isSessionError(error){return /oturum|token|süresi dolmuş/i.test(error?.message||'')}
  function showLoginRequired(target,secondary){localStorage.removeItem('bizimSkorFriendToken');target.innerHTML='<div class="champions-error">Önce mevcut oyuncu hesabınla giriş yap.</div>';if(secondary)secondary.innerHTML=''}

  async function loadRobotPredictions(){const q=await sb.rpc('get_champions_robot_predictions',{p_season:season,p_week:week});if(q.error){console.warn('champions robot',q.error);return{}}return Object.fromEntries((q.data||[]).map(row=>[Number(row.fixture_id),{home_score:Number(row.home_score),away_score:Number(row.away_score)}]))}
  function ensureStatsShell(){if(!document.getElementById('championsWeek2ToolsStyles'))document.head.insertAdjacentHTML('beforeend','<style id="championsWeek2ToolsStyles">.champions-tools{grid-column:1/-1;display:grid;grid-template-columns:1fr 1fr;gap:7px;margin-top:7px}.champions-tool{min-height:36px;padding:8px 7px!important;border:1px solid rgba(147,197,253,.72)!important;border-radius:10px!important;background:rgba(239,246,255,.96)!important;color:#1e3a8a!important;font-size:11px!important;font-weight:900!important}.champions-tool.robot-applied{background:#dcfce7!important;color:#166534!important}.champions-opportunity{grid-column:1/-1;justify-self:center;padding:4px 8px;border-radius:999px;background:#f59e0b;color:#7c2d12;font-size:10px;font-weight:900}.champions-stats-overlay{position:fixed;inset:0;z-index:100001;background:rgba(2,6,23,.78);padding:12px;display:flex;align-items:flex-end;justify-content:center}.champions-stats-overlay.hide{display:none}.champions-stats-modal{width:min(100%,540px);max-height:92vh;overflow:auto;border-radius:22px 22px 0 0;background:#f8fafc;color:#0f172a}.champions-stats-title{padding:15px;background:#071633;color:#fff;text-align:center;font-weight:900}.champions-stats-body{padding:12px}.champions-stats-head{text-align:center;padding:12px;border-radius:14px;background:linear-gradient(135deg,#dbeafe,#fff);font-weight:900}.champions-stat-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:10px}.champions-stat-card,.champions-stat-section{padding:10px;border:1px solid #e2e8f0;border-radius:12px;background:#fff;margin-top:10px}.champions-stat-card span{display:block;font-size:12px;color:#475569}.champions-stat-result{display:grid;grid-template-columns:70px 1fr auto 1fr;gap:5px;padding:6px 0;font-size:11px}.champions-stats-close{width:100%;min-height:52px;background:#071633!important;color:#fff!important}</style>');if(document.getElementById('championsStatsOverlay'))return;document.body.insertAdjacentHTML('beforeend','<div id="championsStatsOverlay" class="champions-stats-overlay hide"><div class="champions-stats-modal" role="dialog" aria-modal="true"><div class="champions-stats-title">📊 Maç İstatistikleri</div><div id="championsStatsBody" class="champions-stats-body"></div><button id="championsStatsClose" type="button" class="champions-stats-close">← Tahmine Dön</button></div></div>');document.getElementById('championsStatsClose').onclick=closeMatchStatistics;document.getElementById('championsStatsOverlay').onclick=e=>{if(e.target===e.currentTarget)closeMatchStatistics()}}
  function toolsMarkup(f){const robot=robotPredictions[f.id],label=robot?`🤖 Robotun Önerisi: ${robot.home_score}-${robot.away_score}`:'🤖 Robotun Önerisi',opportunity=Number(f.id)===CHAMPIONS_OPPORTUNITY_ID?'<span class="champions-opportunity">🔥 Fırsat Maçı • X2</span>':'';return `${opportunity}<div class="champions-tools"><button type="button" class="champions-tool" data-champions-robot="${f.id}" ${robot?'':'disabled'}>${label}</button><button type="button" class="champions-tool" data-champions-stats="${f.id}">📊 Maç İstatistikleri</button></div>`}
  function applyRobot(id,button){const robot=robotPredictions[Number(id)],home=document.getElementById('clh'+id),away=document.getElementById('cla'+id);if(!robot||!home||!away)return;home.value=robot.home_score;away.value=robot.away_score;home.dataset.robotScore=String(robot.home_score);away.dataset.robotScore=String(robot.away_score);button?.classList.add('robot-applied')}
  function bindFixtureActions(host=document){host.querySelectorAll?.('[data-champions-robot]').forEach(button=>button.onclick=e=>{e.preventDefault();applyRobot(button.dataset.championsRobot,button)});host.querySelectorAll?.('[data-champions-stats]').forEach(button=>button.onclick=e=>{e.preventDefault();openMatchStatistics(button.dataset.championsStats)})}
  function dateText(value){return new Intl.DateTimeFormat('tr-TR',{timeZone:'Europe/Istanbul',day:'2-digit',month:'2-digit',year:'numeric'}).format(new Date(value))}
  function statMatches(title,matches){const rows=(matches||[]).slice(0,5);return `<div class="champions-stat-section"><h4>${esc(title)}</h4>${rows.length?rows.map(m=>`<div class="champions-stat-result"><time>${dateText(m.date)}</time><span>${esc(m.home_team)}</span><b>${Number(m.home_score)}-${Number(m.away_score)}</b><span>${esc(m.away_team)}</span></div>`).join(''):'<div>Henüz yeterli maç verisi yok.</div>'}</div>`}
  function renderStatistics(s){const h=s?.home||{},a=s?.away||{};return `<div class="champions-stats-head">${esc(s?.home_team||h.name)} – ${esc(s?.away_team||a.name)}</div><div class="champions-stat-grid"><div class="champions-stat-card"><b>${esc(h.name||s?.home_team)}</b><span>${h.rank?h.rank+'. sıra • ':''}${Number(h.points||0)} puan</span><span>Form: ${(h.form||[]).join(' ')||'—'}</span></div><div class="champions-stat-card"><b>${esc(a.name||s?.away_team)}</b><span>${a.rank?a.rank+'. sıra • ':''}${Number(a.points||0)} puan</span><span>Form: ${(a.form||[]).join(' ')||'—'}</span></div></div>${statMatches((h.name||s?.home_team)+' • Son 5 Maç',h.recent_matches)}${statMatches((a.name||s?.away_team)+' • Son 5 Maç',a.recent_matches)}${statMatches('🤝 Aralarındaki Son 5 Maç',s?.head_to_head)}`}
  async function openMatchStatistics(id){ensureStatsShell();const overlay=document.getElementById('championsStatsOverlay'),body=document.getElementById('championsStatsBody');overlay.classList.remove('hide');document.body.style.overflow='hidden';body.innerHTML='<p>İstatistikler yükleniyor…</p>';const q=await sb.rpc('get_champions_match_statistics',{p_fixture_id:Number(id)});if(q.error){body.textContent=q.error.message||'İstatistikler açılamadı.';return}const snapshot=Array.isArray(q.data)?q.data[0]:q.data;body.innerHTML=snapshot?renderStatistics(snapshot):'<p>Bu maçın istatistikleri henüz hazırlanmadı.</p>'}
  function closeMatchStatistics(){document.getElementById('championsStatsOverlay')?.classList.add('hide');document.body.style.overflow=''}

  function renderInputs(rows,mine){const map=Object.fromEntries(mine.map(x=>[x.fixture_id,x]));return BizimSkorChampionsLeague.groupFixturesByTurkeyDate(rows).map(group=>`<div class="champions-day">${esc(group.label)}</div>`+group.fixtures.map(f=>{const p=map[f.id];return `<div class="champions-match"><div class="t home"><span class="small">${timeText(f.kickoff)}</span><br>${esc(f.home_team)}</div><input id="clh${f.id}" inputmode="numeric" maxlength="2" value="${p?.home_score??''}"><div>-</div><input id="cla${f.id}" inputmode="numeric" maxlength="2" value="${p?.away_score??''}"><div class="t">${esc(f.away_team)}</div>${toolsMarkup(f)}</div>`}).join('')).join('')}
  async function loadPrediction(forceEdit=false){const state=document.getElementById('championsState'),box=document.getElementById('championsFixtures'),save=document.getElementById('championsSave');const token=localStorage.getItem('bizimSkorFriendToken');if(!token){state.innerHTML='<div class="champions-error">Önce mevcut oyuncu hesabınla giriş yap.</div>';box.innerHTML='';save.classList.add('hide');return}state.innerHTML='<p class="small">Şampiyonlar Ligi fikstürü yükleniyor…</p>';const q=await sb.rpc('get_champions_league_week',{p_token:token,p_season:season,p_week:week});if(q.error){if(isSessionError(q.error))showLoginRequired(state,box);else{state.innerHTML=`<div class="champions-error">${esc(q.error.message)}</div>`;box.innerHTML=''}save.classList.add('hide');return}robotPredictions=await loadRobotPredictions();const rows=(q.data||[]).map(rowFixture),mine=rows.filter(x=>x.predicted_home!=null&&x.predicted_away!=null).map(x=>({fixture_id:x.id,home_score:x.predicted_home,away_score:x.predicted_away}));fixtures=rows;const locked=rows.some(x=>x.is_locked);editing=!locked&&(forceEdit||mine.length!==rows.length);if(rows.length&&mine.length===rows.length&&!editing){state.innerHTML=`<div class="champions-summary"><b>✅ Şampiyonlar Ligi 2. hafta tahminlerin kaydedildi</b>${rows.map(f=>{const p=mine.find(x=>x.fixture_id===f.id);return `<div class="savedrow">${esc(f.home_team)} <b>${p.home_score}-${p.away_score}</b> ${esc(f.away_team)}</div>`}).join('')}${locked?'<p class="small">🔒 Tahminler kilitlendi.</p>':'<button id="championsEdit" class="full">Tahminleri Düzenle ✏️</button>'}</div>`;box.innerHTML='';save.classList.add('hide');document.getElementById('championsEdit')?.addEventListener('click',()=>loadPrediction(true));return}if(locked){state.innerHTML='<div class="champions-summary"><b>🔒 Şampiyonlar Ligi 2. hafta tahmin süresi doldu.</b></div>';box.innerHTML='';save.classList.add('hide');return}state.innerHTML='';box.innerHTML=renderInputs(rows,mine);bindFixtureActions(box);save.classList.remove('hide');save.textContent=mine.length===rows.length?'Güncellenmiş Tahminleri Kaydet ✅':'Şampiyonlar Ligi Tahminlerimi Kaydet ⭐'}
  function rankingMarkup(rows){if(!rows.length)return'<div class="champions-summary">Henüz puan oluşmadı.</div>';return `<table><tr><th>Sıra</th><th>Oyuncu</th><th>Puan</th><th>🎯</th><th>⚽</th></tr>${rows.map(row=>{const rank=Number(row.league_rank),medal=rank===1?'🥇 ':rank===2?'🥈 ':rank===3?'🥉 ':'';return `<tr><td>${medal}${rank}</td><td>${esc(row.player_name)}</td><td><b>${row.points}</b></td><td>${row.exact_count}</td><td>${row.correct_count}</td></tr>`}).join('')}</table>`}
  function championsWeeklyRankingMarkup(rows){const list=rows||[];if(!list.length)return'<div class="champions-summary">Şampiyonlar Ligi haftalık sıralaması için henüz tamamlanmış tahmin bulunmadı.</div>';const first=list[0]||{},completed=Number(first.completed_count||0),fixtureCount=Number(first.fixture_count||0),participants=Number(first.participant_count||list.length);return `<div class="champions-summary" style="margin-bottom:10px"><b>🏆 Şampiyonlar Ligi ${week}. Hafta Sıralaması</b><p class="small">${completed}/${fixtureCount} maç sonuçlandı • ${participants} oyuncu listede. Oyuncu adına dokununca haftalık detay açılır.</p></div><table><tr><th>Sıra</th><th>Oyuncu</th><th>Puan</th><th>🎯</th><th>⚽</th></tr>${list.map(row=>{const rank=Number(row.league_rank),medal=rank===1?'🥇 ':rank===2?'🥈 ':rank===3?'🥉 ':'';return `<tr><td>${medal}${rank}</td><td><button type="button" class="bs-player-profile-link" data-player-name="${esc(row.player_name)}" data-profile-week="${week}" data-profile-kind="champions">${esc(row.player_name)}</button></td><td><b>${row.points}</b></td><td>${row.exact_count}</td><td>${row.correct_count}</td></tr>`}).join('')}</table>`}
  function matchResultsMarkup(rows){const matches=BizimSkorChampionsLeague.summarizeMatchResults(rows);if(!matches.length)return'<div class="champions-summary">Henüz maç özeti oluşmadı.</div>';return matches.map(match=>{if(!match.finished)return `<div class="champions-summary" style="margin-bottom:10px"><b>${esc(match.homeTeam)} - ${esc(match.awayTeam)}</b><div class="small">Maç henüz sonuçlanmadı.</div></div>`;const exact=match.exactNames.length?match.exactNames.map(esc).join(', '):'Bilen olmadı';return `<div class="champions-summary" style="margin-bottom:10px"><b>${esc(match.homeTeam)} ${match.realHome}-${match.realAway} ${esc(match.awayTeam)}</b><div class="savedrow">🎯 Tam skor: <b>${exact}</b></div><div class="savedrow">⚽ Sonucu bilen: <b>${match.correctResultCount} kişi</b></div></div>`}).join('')}
  async function loadRanking(){const board=document.getElementById('championsRankingBoard'),results=document.getElementById('championsMatchResults');const token=localStorage.getItem('bizimSkorFriendToken');if(!token){board.innerHTML='<div class="champions-error">Önce mevcut oyuncu hesabınla giriş yap.</div>';results.innerHTML='';return}board.innerHTML='<p class="small">Sıralama yükleniyor…</p>';results.innerHTML='';const [ranking,predictions]=await Promise.all([sb.rpc('get_champions_league_ranking',{p_token:token,p_season:season}),sb.rpc('get_champions_league_week_predictions',{p_token:token,p_season:season,p_week:week})]);if(ranking.error||predictions.error){const error=ranking.error||predictions.error;if(isSessionError(error))showLoginRequired(board,results);else board.innerHTML=`<div class="champions-error">${esc(error.message)}</div>`;return}board.innerHTML=rankingMarkup(ranking.data||[]);results.innerHTML=matchResultsMarkup(predictions.data||[])}
  async function loadChampionsWeeklyRanking(){const board=document.getElementById('championsWeeklyRankingBoard');if(!board)return;const token=localStorage.getItem('bizimSkorFriendToken');if(!token){board.innerHTML='<div class="champions-error">Önce mevcut oyuncu hesabınla giriş yap.</div>';return}board.innerHTML='<p class="small">Şampiyonlar Ligi haftalık sıralama yükleniyor…</p>';const q=await sb.rpc('get_champions_league_weekly_ranking',{p_token:token,p_season:season,p_week:week});if(q.error){if(isSessionError(q.error))showLoginRequired(board);else board.innerHTML=`<div class="champions-error">${esc(q.error.message)}</div>`;return}board.innerHTML=championsWeeklyRankingMarkup(q.data||[]);root.BizimSkorPlayerProfile?.decorateAll?.()}
  function mountWeeklyRankingPanel(){const section=document.getElementById('live'),board=document.getElementById('weeklyRankingBoard');if(!section||!board)return false;if(!document.getElementById('weeklyRankingCompetitionSwitch')){const switcher=document.createElement('div');switcher.id='weeklyRankingCompetitionSwitch';switcher.className='weekly-competition-switch';switcher.innerHTML='<button type="button" class="p" data-weekly-competition="super_lig">Süper Lig</button><button type="button" data-weekly-competition="champions_league">Şampiyonlar Ligi</button><p class="small">Haftalık sıralamada lig seçimi yapabilirsin.</p>';board.insertAdjacentElement('beforebegin',switcher);switcher.addEventListener('click',event=>{const button=event.target.closest?.('button[data-weekly-competition]');if(button)setWeeklyCompetition(button.dataset.weeklyCompetition)});if(!document.getElementById('championsWeeklyRankingStyles'))document.head.insertAdjacentHTML('beforeend','<style id="championsWeeklyRankingStyles">.weekly-competition-switch{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin:10px 0}.weekly-competition-switch button{min-height:46px;background:#e5e7eb;color:#0f172a}.weekly-competition-switch button.p{background:#0f172a;color:#fff}.weekly-competition-switch .small{grid-column:1/-1;margin:0}.champions-weekly-panel{margin-top:10px}</style>')}
    if(!document.getElementById('championsWeeklyRankingPanel')){const panel=document.createElement('div');panel.id='championsWeeklyRankingPanel';panel.className='champions-weekly-panel hide';panel.innerHTML='<div id="championsWeeklyRankingBoard"><p class="small">Şampiyonlar Ligi haftalık sıralama yükleniyor…</p></div>';board.insertAdjacentElement('afterend',panel)}
    setWeeklyCompetition(weeklyCompetition,false);return true}
  function setWeeklyCompetition(kind,shouldLoad=true){weeklyCompetition=kind==='champions_league'?'champions_league':'super_lig';const board=document.getElementById('weeklyRankingBoard'),panel=document.getElementById('championsWeeklyRankingPanel'),switcher=document.getElementById('weeklyRankingCompetitionSwitch'),select=document.getElementById('weeklyRankingWeekSelect');switcher?.querySelectorAll('button[data-weekly-competition]').forEach(button=>button.classList.toggle('p',button.dataset.weeklyCompetition===weeklyCompetition));if(select)select.style.display=weeklyCompetition==='champions_league'?'none':'';if(weeklyCompetition==='champions_league'){if(board)board.style.display='none';panel?.classList.remove('hide');if(shouldLoad)loadChampionsWeeklyRanking()}else{if(board)board.style.display='';panel?.classList.add('hide')}}
  async function loadHistory(historyWeek=week){const content=document.getElementById('historyContent');if(!content)return;const token=localStorage.getItem('bizimSkorFriendToken');if(!token){content.innerHTML='<div class="champions-error">Şampiyonlar Ligi geçmişini görmek için hesabına yeniden giriş yap.</div>';return}content.innerHTML='<p class="small">Şampiyonlar Ligi geçmişi yükleniyor…</p>';const q=await sb.rpc('get_champions_league_history',{p_token:token,p_season:season,p_week:Number(historyWeek)});if(q.error){if(isSessionError(q.error))showLoginRequired(content);else content.innerHTML=`<div class="champions-error">${esc(q.error.message)}</div>`;return}const rows=q.data||[];if(!rows.length){content.innerHTML='<p class="small">Şampiyonlar Ligi hafta bilgisi bulunamadı.</p>';return}const summary=rows[0],completed=Number(summary.completed_count||0),total=Number(summary.fixture_count||rows.length);const finished=total>0&&completed===total,rank=completed&&summary.week_rank?`${summary.week_rank}.`:'-';const matches=rows.map(row=>{const prediction=row.predicted_home==null||row.predicted_away==null?null:{home_score:row.predicted_home,away_score:row.predicted_away};const result=row.real_home==null||row.real_away==null?null:{home_score:row.real_home,away_score:row.real_away};const score=BizimSkorChampionsLeague.scorePrediction(prediction,result);const mark=score.symbol?`${score.symbol} +${score.points}`:'';const predictionText=prediction?`${prediction.home_score}-${prediction.away_score}`:'Tahmin bulunamadı';return `<div class="history-match"><div class="history-real">${esc(row.home_team)} ${result?`<b>${result.home_score}-${result.away_score}</b>`:'-'} ${esc(row.away_team)}</div><div class="history-pred">Tahminin: <b>${predictionText}</b><span class="history-mark">${mark}</span></div></div>`}).join('');content.innerHTML=`<div class="history-summary"><div class="history-stat"><span>Haftalık Puan</span><b>${summary.week_points||0}</b></div><div class="history-stat"><span>${finished?'Hafta Sırası':'Geçici Sıra'}</span><b>${rank}</b></div><div class="history-stat"><span>Katılan</span><b>${summary.participant_count||0}</b></div></div><p class="small">${completed}/${total} maç sonuçlandı. ${finished?`Şampiyonlar Ligi ${historyWeek}. haftayı ${rank} sırada tamamladın.`:'Sıralama maçlar sonuçlandıkça güncellenir.'}</p>${matches}`}
  async function savePrediction(){const token=localStorage.getItem('bizimSkorFriendToken');if(!token)return alert('Önce mevcut oyuncu hesabınla giriş yap.');try{const rows=fixtures.map(f=>{const home=document.getElementById('clh'+f.id),away=document.getElementById('cla'+f.id);return{fixture_id:f.id,home_score:home?.value,away_score:away?.value,robot_applied:home?.dataset.robotScore===home?.value&&away?.dataset.robotScore===away?.value}});const checked=BizimSkorChampionsLeague.validateWeeklyScores(fixtures,rows);const q=await sb.rpc('save_champions_league_predictions',{p_token:token,p_season:season,p_week:week,p_predictions:checked});if(q.error)throw q.error;alert('Şampiyonlar Ligi tahminlerin kaydedildi ✅');editing=false;await loadPrediction()}catch(error){alert(error.message)}}
  window.BizimSkorChampionsUI={mount,currentWeek,openPrediction,loadPrediction,loadRanking,loadChampionsWeeklyRanking,mountWeeklyRankingPanel,setWeeklyCompetition,championsWeeklyRankingMarkup,loadHistory,openMatchStatistics,getFixtures:()=>[...fixtures]};
  mount();
})();
