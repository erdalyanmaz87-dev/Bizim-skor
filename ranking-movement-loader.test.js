const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const loader=require('./ui-integration-loader');
test('siralamada hareket katmani bootstrap sirasi ile yuklenir',()=>{const order=loader.scriptOrder(),a=order.indexOf('ranking-movement.js'),b=order.indexOf('ranking-movement-bootstrap.js'),c=order.indexOf('ranking-movement-ui.js');assert(a>=0);assert(a<b);assert(b<c)});
test('koyu oyuncu karti stili loader tarafindan yuklenir',()=>{assert.match(fs.readFileSync('./ui-integration-loader.js','utf8'),/admin-player-card-dark-mode\.css/);assert.match(fs.readFileSync('./admin-player-card-dark-mode.css','utf8'),/\.bs-admin-player-grid>div\{background:#f8fafc!important;color:#0f172a!important/)});
