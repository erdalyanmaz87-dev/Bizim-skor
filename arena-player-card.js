(function(root,factory){const api=factory(root);if(typeof module==='object'&&module.exports)module.exports=api;else{root.BizimSkorArenaPlayerCard=api;api.mount();}})(typeof globalThis!=='undefined'?globalThis:this,function(root){
const LABELS={champions:'Şampiyonlar',elite:'Elit Lig',gold:'Altın Lig',silver:'Gümüş Lig',bronze:'Bronz Lig'};
const ICONS={champions:'👑',elite:'💎',gold:'🥇',silver:'🥈',bronze:'🥉'};
function esc(v){return String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#39;')}
function fmt(v){return v==null||v===''?'—':Number(v).toFixed(2)}
function leagueLabel(code){return LABELS[code]||'Bronz Lig'}
function statusText(data={}){if(!data.is_eligible){const n=Math.max(1,Number(data.rounds_needed)||2-Math.min(2,Number(data.valid_round_count)||0));return `⚠️ ${n} tur daha gerekli`}if(data.promotion_status==='promotion')return'🟢 Yükselme hattında';if(data.promotion_status==='relegation')return'🔴 Düşme hattında';if(data.promotion_status==='championship')return'🏆 Liderlik koltuğunda';return'🟡 Güvenli bölgede'}
function previewStatusText(data={}){const n=Math.max(0,Number(data.rounds_needed)||2-Math.min(2,Number(data.valid_round_count)||0));return n===0?'✅ 2 başlangıç haftası tamamlandı':`⚠️ ${n} başlangıç haftası eksik`}
function roundLabel(row={}){const week=String(row.round_key||'').split(':').pop();if(row.competition==='super_lig')return `Süper Lig ${esc(week)}. Hafta`;if(row.competition==='champions_league')return `Şampiyonlar Ligi ${esc(week)}. Tur`;if(row.competition==='nations_league')return `Uluslar Ligi ${esc(week)}. Tur`;return esc(row.round_key||'Tahmin turu')}
function renderRounds(rounds=[]){if(!rounds.length)return '<div class="arena-card-empty">Henüz tamamlanmış Arena turu yok.</div>';return `<div class="arena-card-rounds">${rounds.map(r=>`<div class="arena-card-round"><div><b>${roundLabel(r)}</b><small>${esc(r.rank??'—')}. sıra / ${esc(r.participant_count??'—')} oyuncu</small></div><strong>${fmt(r.performance_score)}</strong></div>`).join('')}</div>`}
function renderPlayerCard(data={}){const code=data.league_code||'bronze',rank=data.league_rank??data.rank_in_league??'—',size=data.league_size??'—';return `<div class="arena-card-backdrop" data-arena-card-close="1"><section class="arena-player-card" role="dialog" aria-modal="true" aria-label="Arena oyuncu kartı"><button class="arena-card-close" type="button" data-arena-card-close="1" aria-label="Kapat">×</button><div class="arena-card-kicker">BİZİM SKOR ARENA</div><h3>${ICONS[code]||'🥉'} ${esc(data.player_name||'Oyuncu')}</h3><div class="arena-card-league">${esc(leagueLabel(code))}</div><div class="arena-card-grid"><div><span>Lig sırası</span><b>${esc(rank)} / ${esc(size)}</b></div><div><span>Arena puanı</span><b>${fmt(data.performance_score)}</b></div><div><span>Geçerli tur</span><b>${esc(data.valid_round_count??0)}</b></div><div><span>Durum</span><b>${esc(statusText(data))}</b></div></div><div class="arena-card-section-title">Tur performansları</div>${renderRounds(data.rounds||[])}<div class="arena-card-note">Arena dönem puanı, geçerli tur performans puanlarının aritmetik ortalamasıdır.</div></section></div>`}
function renderPreviewCard(data={}){const code=data.league_code||'bronze';return `<div class="arena-card-backdrop" data-arena-card-close="1"><section class="arena-player-card arena-player-card-preview" role="dialog" aria-modal="true" aria-label="Geçici Arena oyuncu kartı"><button class="arena-card-close" type="button" data-arena-card-close="1" aria-label="Kapat">×</button><div class="arena-card-kicker">Geçici başlangıç</div><h3>${ICONS[code]||'🥉'} ${esc(data.player_name||'Oyuncu')}</h3><div class="arena-card-league">${esc(leagueLabel(code))}</div><div class="arena-card-grid"><div><span>Lig sırası</span><b>${esc(data.league_rank??'—')} / ${esc(data.league_size??'—')}</b></div><div><span>Başlangıç ortalaması</span><b>${fmt(data.performance_score)}</b></div><div><span>3. Hafta</span><b>${fmt(data.week3_score??0)}</b></div><div><span>4. Hafta</span><b>${fmt(data.week4_score??0)}</b></div><div><span>Katılım</span><b>${esc(data.valid_round_count??0)} / 2</b></div><div><span>Durum</span><b>${esc(previewStatusText(data))}</b></div></div><div class="arena-card-note">Başlangıç puanı = (3. hafta + 4. hafta) / 2. Tahmin yapılmayan başlangıç haftası bu geçici hesapta 0 kabul edilir. 5. hafta başladığında Arena dönem puanı sıfırdan hesaplanır.</div></section></div>`}
function css(){return `.league-row{cursor:pointer}.arena-card-backdrop{position:fixed;inset:0;z-index:99999;background:rgba(15,23,42,.55);display:flex;align-items:flex-end;justify-content:center;padding:16px}.arena-player-card{position:relative;width:min(520px,100%);max-height:82vh;overflow:auto;background:#fff;border-radius:22px 22px 14px 14px;padding:20px;box-shadow:0 24px 80px rgba(15,23,42,.35);color:#0f172a}.arena-card-close{position:absolute;right:14px;top:12px;width:34px;height:34px;border-radius:50%;border:0;background:#f1f5f9;font-size:24px;line-height:1}.arena-card-kicker{font-size:11px;font-weight:900;letter-spacing:.12em;color:#64748b}.arena-player-card h3{margin:6px 42px 2px 0;font-size:24px}.arena-card-league{font-size:13px;font-weight:900;color:#475569;margin-bottom:14px}.arena-card-grid{display:grid;grid-template-columns:1fr 1fr;gap:9px}.arena-card-grid>div{border:1px solid #e2e8f0;border-radius:13px;padding:10px;background:#f8fafc}.arena-card-grid span{display:block;font-size:11px;color:#64748b;font-weight:800;margin-bottom:4px}.arena-card-grid b{font-size:15px}.arena-card-section-title{margin:16px 0 8px;font-weight:900}.arena-card-round{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:10px 0;border-bottom:1px solid #eef2f7}.arena-card-round small{display:block;color:#64748b;margin-top:3px}.arena-card-round strong{font-size:18px}.arena-card-empty{padding:14px;border-radius:12px;background:#f8fafc;color:#64748b;text-align:center}.arena-card-note{margin-top:14px;padding:11px 12px;border-radius:12px;background:#eff6ff;color:#1e3a8a;font-size:12px;line-height:1.45;font-weight:700}.arena-player-card-preview .arena-card-note{background:#fffbeb;color:#92400e}`}
function ensureStyle(){if(typeof document==='undefined'||document.getElementById('arena-player-card-style'))return;const s=document.createElement('style');s.id='arena-player-card-style';s.textContent=css();document.head.appendChild(s)}
function removeCard(){root.document?.querySelector('.arena-card-backdrop')?.remove()}
function playerNameFromRow(row){const t=row?.querySelector('.league-player')?.textContent||'';return t.replace(/^\s*Sen\s*•\s*/,'').trim()}
function previewDataFromRow(row,name){const shell=row.closest('.league-preview-shell'),league=Object.entries(LABELS).find(([,v])=>shell?.querySelector('h2')?.textContent?.includes(v))?.[0]||'bronze',rows=[...shell.querySelectorAll('.league-row')];return{player_name:name,league_code:league,league_rank:row.querySelector('.league-rank')?.textContent?.trim(),league_size:rows.length,performance_score:row.querySelector('.league-performance')?.textContent?.trim()}}
async function openForRow(row){const name=playerNameFromRow(row);if(!name)return;removeCard();const token=root.localStorage?.getItem('bizimSkorFriendToken');const sb=root.sb;if(row.closest('.league-preview-shell')){let data=previewDataFromRow(row,name);if(token&&sb?.rpc){const res=await sb.rpc('get_arena_initial_player_card',{p_token:token,p_player_name:name});if(!res.error){const detail=Array.isArray(res.data)?res.data[0]:res.data;if(detail)data={...data,...detail}}}root.document.body.insertAdjacentHTML('beforeend',renderPreviewCard(data));return}if(!token||!sb?.rpc)return;const res=await sb.rpc('get_arena_player_card',{p_token:token,p_player_name:name});if(res.error)throw res.error;const data=Array.isArray(res.data)?res.data[0]:res.data;if(data)root.document.body.insertAdjacentHTML('beforeend',renderPlayerCard(data))}
function mount(){if(typeof document==='undefined')return;ensureStyle();document.addEventListener('click',e=>{const close=e.target.closest?.('[data-arena-card-close]');if(close){if(close.classList?.contains('arena-card-backdrop')&&e.target!==close)return;removeCard();return}const row=e.target.closest?.('#leagueSystemPanel .league-row');if(row)openForRow(row).catch(err=>console.warn('arena player card',err))});document.addEventListener('keydown',e=>{if(e.key==='Escape')removeCard()})}
return Object.freeze({esc,statusText,previewStatusText,roundLabel,renderPlayerCard,renderPreviewCard,css,mount});
});
