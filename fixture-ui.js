(function(){
 function styles(){if(document.getElementById('fixtureStyles'))return;document.head.insertAdjacentHTML('beforeend','<style id="fixtureStyles">.fixture-controls{display:grid;grid-template-columns:1fr 1fr;gap:8px}.fixture-day{margin:14px 0 5px;padding:7px 10px;border-radius:10px;background:#eff6ff;color:#1e3a8a;font-size:12px;font-weight:900;text-transform:capitalize}.fixture-game{display:grid;grid-template-columns:48px minmax(0,1fr) 58px minmax(0,1fr);gap:7px;align-items:center;padding:9px 0;border-bottom:1px solid #e5e7eb}.fixture-time{font-size:11px;color:#64748b;font-weight:800}.fixture-team{text-align:right;font-size:12px;font-weight:750}.fixture-team.away{text-align:left}.fixture-score{text-align:center;font-weight:900;white-space:nowrap}@media(max-width:430px){.fixture-controls{grid-template-columns:1fr}.fixture-game{grid-template-columns:42px minmax(0,1fr) 50px minmax(0,1fr);gap:5px}}</style>')}
 function prepare(){styles();const tab=document.querySelector('[data-tab="resultsWeek"]'),section=document.getElementById('resultsWeek'),card=section?.querySelector('.c');if(!tab||!card)return;tab.textContent='📅 Fikstür';card.querySelector('h2').textContent='📅 Fikstür';if(!document.getElementById('fixtureCompetition')){const select=document.getElementById('resultsWeekSelect');select.insertAdjacentHTML('beforebegin','<div class="fixture-controls"><select id="fixtureCompetition" class="history-select"><option value="super">🇹🇷 Süper Lig</option><option value="champions">⭐ Şampiyonlar Ligi</option></select><div id="fixtureWeekSlot"></div></div>');document.getElementById('fixtureWeekSlot').appendChild(select);document.getElementById('fixtureCompetition').onchange=()=>loadResultsWeeks()}}
 function render(rows,resultRows){const map=Object.fromEntries((resultRows||[]).map(r=>[r.fixture_id,r]));return BizimSkorFixture.groupByTurkeyDay(rows).map(group=>`<div class="fixture-day">${esc(group.label)}</div>${group.rows.map(f=>{const r=map[f.id],done=r&&r.home_score!=null&&r.away_score!=null;return `<div class="fixture-game"><div class="fixture-time">${BizimSkorFixture.turkeyTime(f.kickoff)}</div><div class="fixture-team">${esc(f.home_team)}</div><div class="fixture-score">${done?`${r.home_score} - ${r.away_score}`:'- -'}</div><div class="fixture-team away">${esc(f.away_team)}</div></div>`}).join('')}`).join('')||'<p class="small">Bu haftaya ait maç bulunamadı.</p>'}
 window.loadResultsWeeks=async function(preferredWeek){prepare();const comp=document.getElementById('fixtureCompetition')?.value||'super',select=document.getElementById('resultsWeekSelect'),list=document.getElementById('resultsList');if(comp==='champions'){const fq=await sb.from('champions_league_fixtures').select('*').eq('season','2026/27').order('week',{ascending:false}).order('kickoff');if(fq.error){list.innerHTML='<p class="small">Fikstür yüklenemedi.</p>';return}const weeks=[...new Set((fq.data||[]).map(x=>+x.week))].sort((a,b)=>b-a),chosen=weeks.includes(+preferredWeek)?+preferredWeek:(weeks[0]||1);select.innerHTML=weeks.map(w=>`<option value="${w}" ${w===chosen?'selected':''}>${w}. Hafta</option>`).join('');select.onchange=()=>renderFixtureWeek(+select.value);await renderFixtureWeek(chosen);return}const fq=await sb.from('fixtures').select('*').order('week',{ascending:false}).order('kickoff');if(fq.error){list.innerHTML='<p class="small">Fikstür yüklenemedi.</p>';return}const weeks=[...new Set((fq.data||[]).map(x=>+x.week))].sort((a,b)=>b-a),chosen=weeks.includes(+preferredWeek)?+preferredWeek:weeks[0];select.innerHTML=weeks.map(w=>`<option value="${w}" ${w===chosen?'selected':''}>${w}. Hafta</option>`).join('');select.onchange=()=>renderFixtureWeek(+select.value);await renderFixtureWeek(chosen)};
 window.renderFixtureWeek=async function(selectedWeek){const comp=document.getElementById('fixtureCompetition')?.value||'super',list=document.getElementById('resultsList');list.innerHTML='<p class="small">Fikstür yükleniyor…</p>';const table=comp==='champions'?'champions_league_fixtures':'fixtures',resultTable=comp==='champions'?'champions_league_results':'results';let query=sb.from(table).select('*').eq('week',selectedWeek).order('kickoff');if(comp==='champions')query=query.eq('season','2026/27');const fq=await query;if(fq.error){list.innerHTML='<p class="small">Fikstür yüklenemedi.</p>';return}const rows=fq.data||[],ids=rows.map(f=>f.id),rq=ids.length?await sb.from(resultTable).select('*').in('fixture_id',ids):{data:[],error:null};if(rq.error){list.innerHTML='<p class="small">Sonuçlar yüklenemedi.</p>';return}list.innerHTML=render(rows,rq.data||[])};
 window.addEventListener('load',()=>setTimeout(prepare,300));
})();