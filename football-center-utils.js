(function(){
  const CATEGORIES=Object.freeze(['standings','top_scorers','top_assists','availability']);
  const COMPETITIONS=Object.freeze(['super_lig','champions_league']);
  const esc=value=>String(value??'').replace(/[&<>'\"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','\"':'&quot;'}[char]));
  const number=value=>Number.isFinite(Number(value))?Number(value):0;
  function footballCenterThemeClass(competition){return competition==='champions_league'?'football-center-champions':''}
  function formatUpdatedAt(value){const date=new Date(value);if(Number.isNaN(date.getTime()))return 'Henüz güncellenmedi';return new Intl.DateTimeFormat('tr-TR',{dateStyle:'short',timeStyle:'short',timeZone:'Europe/Istanbul'}).format(date)}
  function buildViewModel(rows,competition,category){if(!COMPETITIONS.includes(competition))throw new Error('Geçersiz organizasyon');if(!CATEGORIES.includes(category))throw new Error('Geçersiz kategori');const row=(Array.isArray(rows)?rows:[]).find(item=>item&&item.category===category);const payload=row?.payload;if(category==='availability'){const items=Array.isArray(payload?.items)?payload.items:[];return{competition,category,updatedAt:row?.fetched_at||null,coverage:payload?.coverage===true,items}}return{competition,category,updatedAt:row?.fetched_at||null,items:Array.isArray(payload)?payload:[]}}
  function updated(model){return `<p class="football-center-updated">Son güncelleme: ${esc(formatUpdatedAt(model.updatedAt))}</p>`}
  function renderStandings(items){const body=items.map(item=>`<tr><td>${number(item.rank)}</td><td>${esc(item.team)}</td><td>${number(item.played)}</td><td>${number(item.goal_diff)}</td><td><strong>${number(item.points)}</strong></td></tr>`).join('');return `<div class="football-center-table-wrap"><table><thead><tr><th>Sıra</th><th>Takım</th><th>O</th><th>AV</th><th>P</th></tr></thead><tbody>${body}</tbody></table></div>`}
  function renderLeaders(items,label){const body=items.map((item,index)=>`<tr><td>${number(item.rank)||index+1}</td><td>${esc(item.player)}</td><td>${esc(item.team)}</td><td><strong>${number(item.value)}</strong></td></tr>`).join('');return `<div class="football-center-table-wrap"><table><thead><tr><th>Sıra</th><th>Oyuncu</th><th>Takım</th><th>${label}</th></tr></thead><tbody>${body}</tbody></table></div>`}
  function availabilityCards(items){return items.length?items.map(item=>`<article class="football-player-card"><strong>${esc(item.player)}</strong><span>${esc(item.team)}</span></article>`).join(''):'<p class="football-center-empty">Şu anda listelenecek oyuncu bulunmuyor.</p>'}
  function renderFootballCenterMarkup(model){if(model?.error)return '<p class="football-center-error">Veriler şu anda yüklenemedi. Tekrar deneyebilirsin.</p>';if(model.category==='availability'){if(!model.coverage)return `<p class="football-center-empty">Bu organizasyon için oynayamayacak oyuncu veri kapsamı bulunamadı.</p>${updated(model)}`;return `<h3>Oynaması Beklenmeyen Oyuncular</h3>${availabilityCards(model.items||[])}<p class="football-center-empty">⚠️ Bilgiler muhtemeldir, kesin değildir.</p>${updated(model)}`}const items=model.items||[];if(!items.length)return `<p class="football-center-empty">Henüz güncel veri bulunmuyor.</p>${updated(model)}`;const content=model.category==='standings'?renderStandings(items):renderLeaders(items,model.category==='top_scorers'?'Gol':'Asist');return content+updated(model)}
  const api=Object.freeze({CATEGORIES,buildViewModel,renderFootballCenterMarkup,formatUpdatedAt,footballCenterThemeClass});
  if(typeof window!=='undefined')window.BizimSkorFootballCenter=api;
  if(typeof module!=='undefined')module.exports=api;
})();
